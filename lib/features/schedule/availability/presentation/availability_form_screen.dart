import 'package:flutter/material.dart';

class AvailabilityFormScreen extends StatelessWidget {
  const AvailabilityFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Set Availability')),
      body: const Center(child: Text('Availability form coming soon...')),
    );
  }
}
